import theme from "../theme"

const { colors } = theme

export const whiteTheme = {
  body: colors.veryLigthtGrey,
  headerColor: colors.DarkGray,
  textColor: colors.dark,
  buttonColor: colors.lightGray,
  backgroundHover: colors.middleGrey,
}
export const blackTheme = {
  body: colors.middleGrey,
  headerColor: colors.DarkGray,
  textColor: colors.veryLigthtGrey,
  buttonColor: colors.lightGray,
  colorNumbers: colors.dark,
  backgroundHover: colors.DarkGray,
}
export const colorTheme = {
  body: colors.warmBeige,
  headerColor: colors.violet,
  textColor: colors.beige,
  buttonColor: colors.darkBeige,
  colorNumbers: colors.darkBlue,
  backgroundHover: colors.darkPink,
}
